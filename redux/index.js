const applyMiddleware = require('./applyMiddleware');
const combineReducers = require('./combineReducers');
const creatStore = require('./creatStore');

module.exports = {
    applyMiddleware,
    combineReducers,
    creatStore
};
